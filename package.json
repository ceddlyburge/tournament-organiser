{
  "private": true,
  "scripts": {
    "postinstall": "elm-tooling install",
    "format": "npx elm-format --validate .",
    "review": "npx elm-review",
    "test": "npx elm-test",
    "build": "elm-app build",
    "check-and-build": "npm run format && npm run review && npm run test && npm run build"
  },
  "devDependencies": {
    "create-elm-app": "^3.2.2",
    "elm-review": "^2.9.1",
    "elm-tooling": "^1.13.1"
  }
}
