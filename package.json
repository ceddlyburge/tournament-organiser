{
  "private": true,
  "scripts": {
    "format": "elm-format --validate .",
    "review": "npx elm-review --compiler ./node_modules/elm/bin/elm",
    "test": "elm-app test",
    "build": "elm-app build",
    "check-and-build": "npm run format && npm run review && npm run test && npm run build"
  },
  "devDependencies": {
    "create-elm-app": "^5.2.2",
    "elm-format": "^0.8.6"
  },
  "dependencies": {
    "elm": "^0.19.1-5"
  }
}
